@import url('https://fonts.googleapis.com/css2?family=Sora:wght@400;600&display=swap');

body {
  margin: 0;
  padding: 0;
  font-family: 'Sora', sans-serif;
}

/* The map container fills the viewport with a solid dark background */
#map {
  position: absolute;
  top: 0;
  bottom: 0;
  width: 100%;
  background: #000B2D;
}

/* Stack icon over label, center-aligned */
.marker {
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* Label below icon in bright blue, Sora 600 */
.marker span {
  margin-top: 4px;
  color: #FFFFFF;
  -webkit-text-stroke: 0.25px #0935B8;
  font-family: 'Sora', sans-serif;
  font-weight: 1;
  font-size: 11px;
  white-space: nowrap;
}
